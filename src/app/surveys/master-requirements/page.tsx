import { PasswordProtect } from '@/components/password-protect';

export default function MasterRequirementsSurveyPage() {
  return (
    <PasswordProtect>
      <div className="min-h-screen w-full">
        {/* This screen is intentionally blank for now */}
      </div>
    </PasswordProtect>
  );
}
